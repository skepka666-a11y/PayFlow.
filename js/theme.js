// Получаем элементы const html = document.getElementById('html'); const themeToggle = document.getElementById('theme-toggle'); const themeToggleMobile = document.getElementById('theme-toggle-mobile'); // Функция переключения темы function toggleTheme() { html.classList.toggle('dark'); localStorage.theme = html.classList.contains('dark') ? 'dark' : 'light'; } // Устанавливаем тему при загрузке if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) { html.classList.add('dark'); } else { html.classList.remove('dark'); } // Назначаем обработчики if (themeToggle) themeToggle.addEventListener('click', toggleTheme); if (themeToggleMobile) themeToggleMobile.addEventListener('click', toggleTheme);